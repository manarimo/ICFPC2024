-- Make recursive Solve
B$
Lf
    B$ B$ vf vf @I83
-- Solve :: Self -> Int -> String
Ls Lp
    ? (B= vp @I21)
    @Ssolve@_lambdaman15@_
    B.
      B$ B$ vs vs (B% (B* @I55 vp) @I824843)
      B. ? (B= vp @I361178)
	   @SDDRRUUUUUULLRRDDDDDDLLUU
           ? (B= vp @I251069)
	     @SUUDD
	     ? (B= vp @I571954) @SUUDD S
      BT @I2 BD B* @I2 (B% vp @I4) @SUUDDRRLL
