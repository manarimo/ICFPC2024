-- Make recursive Solve
B$
Lf
    B$ B$ vf vf @I45
-- Solve :: Self -> Int -> String
Ls Lp
    ? (B= vp @I5)
    @Ssolve@_lambdaman20@_
    B. B.
      B$ B$ vs vs (B% (B* @I3 vp) @I994193)
       ? (B= vp @I164969) @SLURD
       ? (B= vp @I882308) @SDRLU S
      BT @I1 BD (B% vp @I4) @SUDRL
